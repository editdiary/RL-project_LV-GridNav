# LV-GridNav: Language-Vision Guided Grid Navigation with Reinforcement Learning

## 브랜치 설명
이 브랜치(policy)는 프로젝트의 초기 구현 단계에서 Policy Iteration 알고리즘을 구현한 코드를 보존하기 위한 브랜치입니다. 이후 Q-Learning과 DQN 알고리즘의 구현 과정에서 전체적인 코드 구조가 크게 변경되기 때문에, main branch에서 이 코드를 직접 실행하기 위해서는 추가적인 수정이 필요합니다.

최신 구현 코드는 `main` 브랜치에서 확인하실 수 있습니다.


## 주요 기능

### 환경 구성
- (N x N) 크기의 비정형 그리드 환경 (기본값: 7x7)
- 패딩 영역 포함 (기본값: 2)
- 4방향 이동 가능 (상, 하, 좌, 우)
- 다양한 지형 특성:
  - 일반 경로
  - 공사중인 경로 (이동 가능하지만 상대적으로 높은 패널티 - 일반길의 3배)
  - 벽 (이동 불가능한 영역)

### 에이전트 특성
- Policy Iteration 알고리즘 구현:
  - 전체 MDP를 안다는 가정 하에 Value 기반 정책 학습
  - Value Iteration을 통한 최적 정책 도출
  - 이 구현은 프로젝트의 초기 단계에서 수행되었으며, 현재는 `main` 브랜치에서 더 발전된 알고리즘들이 구현되어 있습니다.

### 시나리오
1. **단일 목표 도달 (시나리오1)**: 랜덤하게 지정된 단일 목표 지점까지 최단 시간 내 도달
   - 목표 지점은 에이전트와 최소 5칸 이상 떨어져 있도록 설정
   - 목표 도달 시 큰 보상 제공
   - (집을 빨리 가야해..!)

2. **최근접 목표 도달 (시나리오2)**: 여러 목표 지점 중 가장 가까운 지점을 선택하여 최단 시간 내 도달
   - 3개의 목표 지점 생성
   - 목표 지점들 사이의 최소 거리 3칸 유지
   - 가장 가까운 목표 지점 도달 시 보상 제공
   - (다이소를 가야 하는데, 여러 지점 중에서 가능하면 가장 빨리 갈 수 있는 곳에 가고 싶어..!)

3. **순회 목표 도달 (시나리오3)**: 여러 목표 지점을 모두 방문하는 최적 경로 학습
   - 3개의 목표 지점 생성
   - 목표 지점들 사이의 최소 거리 3칸 유지
   - 방문한 목표 지점 수에 따라 보상 증가
   - 모든 목표 지점 방문 시 추가 보상 제공
   - ( [다이소=>올리브영=>집]을 가장 빨리 거쳐서 가고 싶어..!)

### 보상 체계 (main branch와 차이가 있음)
- 기본 이동: -1.0 (STEP_PENALTY)
- 벽 충돌: -5.0 (WALL_PENALTY)
- 공사중인 경로: -2.0 (CONSTRUCTION_PENALTY)
- 목표 도달: +10.0 (GOAL_REWARD)

## 주요 라이브러리 버전
```
Python: 3.11
NumPy: 2.1.2
PyTorch: 2.5.0+cu121
Matplotlib
```

## 참고사항
이 브랜치의 코드는 프로젝트의 초기 구현 단계를 보존하기 위한 것이며, 현재는 `main` 브랜치에서 더 발전된 형태의 구현이 진행되고 있습니다. Policy Iteration 알고리즘의 구현을 참고하시려면 `runs/PolicyIteration.ipynb` 파일을 확인해 주시기 바랍니다.